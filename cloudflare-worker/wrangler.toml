name = "lumera-crash-reporter"
main = "worker.js"
compatibility_date = "2024-01-01"

# Repo that crash reports are filed against as GitHub issues.
# GITHUB_TOKEN, AUTH_TOKEN, RESEND_API_KEY, and REPORT_EMAIL are secrets —
# set them with `npx wrangler secret put <NAME>`, never here.
[vars]
GITHUB_OWNER = "HereLiesAz"
GITHUB_REPO = "illumera"
